// const express = require("express");
// const { createCheif, loginCheif, getCheif } = require("../controllers/cheif.Controller");
// const { protect } = require("../middlewares/authMiddleware");

// const router = express.Router();

// router.post("/create", protect, createCheif);
// router.get("/", protect, getCheif);
// router.post("/", protect, createCheif);
// router.post("/login", loginCheif);

// module.exports = router;