// const express = require("express");
// const { createInventorymanager, loginInventorymanager, getInventorymanager } = require("../controllers/inventorymanager.Controller");
// const { protect } = require("../middlewares/authMiddleware");

// const router = express.Router();

// router.post("/create", protect, createInventorymanager);
// router.get("/", protect, getInventorymanager);
// router.post("/", protect, createInventorymanager);
// router.post("/login", loginInventorymanager);

// module.exports = router;