// const express = require("express");
// const { createCleaner, loginCleaner, getCleaner } = require("../controllers/cleaner.Controller");
// const { protect } = require("../middlewares/authMiddleware");

// const router = express.Router();

// router.post("/create", protect, createCleaner);
// router.get("/", protect, getCleaner);
// router.post("/", protect, createCleaner);
// router.post("/login", loginCleaner);

// module.exports = router;