// const express = require("express");
// const { createSucheif, loginSucheif, getSucheif } = require("../controllers/sucheif.Controller");
// const { protect } = require("../middlewares/authMiddleware");

// const router = express.Router();

// router.post("/create", protect, createSucheif);
// router.get("/", protect, getSucheif);
// router.post("/", protect, createSucheif);
// router.post("/login", loginSucheif);

// module.exports = router;