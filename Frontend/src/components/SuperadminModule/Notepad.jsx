import React from "react";

import RoleNotesPage from "../common/RoleNotesPage";

const Notepad = () => (
  <RoleNotesPage
    title="Super Admin Personal Notes"
    description="Keep platform reminders, pinned priorities, and admin notes easy to review."
  />
);

export default Notepad;
