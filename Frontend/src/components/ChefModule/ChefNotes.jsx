import React from "react";

import RoleNotesPage from "../common/RoleNotesPage";

const ChefNotes = () => (
  <RoleNotesPage
    title="Chef Personal Notes"
    description="Keep prep notes, kitchen reminders, and pinned priorities easy to find."
  />
);

export default ChefNotes;
