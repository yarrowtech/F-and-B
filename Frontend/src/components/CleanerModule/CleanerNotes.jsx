import React from "react";

import LocalNotesPage from "../common/LocalNotesPage";

const CleanerNotes = () => (
  <LocalNotesPage
    storageKey="Notes"
    title="Cleaner Personal Notes"
    description="Track cleaning reminders and quick task notes for the current shift."
  />
);

export default CleanerNotes;
