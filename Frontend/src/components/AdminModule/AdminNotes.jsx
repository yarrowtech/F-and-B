import React from "react";

import RoleNotesPage from "../common/RoleNotesPage";

const AdminNotes = () => (
  <RoleNotesPage
    title="Admin Personal Notes"
    description="Pin admin reminders, search saved notes, and keep daily follow-ups close."
  />
);

export default AdminNotes;
