import React from "react";

import RoleNotesPage from "../common/RoleNotesPage";

const WaiterNotes = () => (
  <RoleNotesPage
    title="Waiter Personal Notes"
    description="Save table reminders, service notes, and follow-ups for the shift."
  />
);

export default WaiterNotes;
