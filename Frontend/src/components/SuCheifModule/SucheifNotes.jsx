import React from "react";

import LocalNotesPage from "../common/LocalNotesPage";

const SucheifNotes = () => (
  <LocalNotesPage
    storageKey="sucheifPersonalNotes"
    title="Su Chef Personal Notes"
    description="Keep prep reminders, handoff notes, and shift details in one place."
  />
);

export default SucheifNotes;
